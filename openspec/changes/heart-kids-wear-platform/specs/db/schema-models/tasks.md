# Tasks: Database Schema & Relational Models (DB)

- [x] 1.1 Configure SQLite database engine with WAL mode and `PRAGMA foreign_keys = ON;` in `backend/app/database.py`
- [x] 1.2 Implement `members`, `shipping_addresses`, and `member_events` SQLAlchemy models in `backend/app/models/user.py`
- [x] 1.3 Implement `categories`, `group_campaigns`, `products`, `product_variants`, and `product_images` SQLAlchemy models in `backend/app/models/product.py`
- [x] 1.4 Implement `orders`, `order_items`, `payment_records`, `cart_items`, and `wishlist` SQLAlchemy models in `backend/app/models/order.py`
- [x] 1.5 Implement `messages`, `message_templates`, `points_cards`, `expense_ledger`, `income_ledger`, and `system_config` SQLAlchemy models in `backend/app/models/finance.py`
- [x] 1.6 Verify table generation and foreign key constraints on SQLite
